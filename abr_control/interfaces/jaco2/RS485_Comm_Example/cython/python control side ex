cout << "RS-485 communication Example." << endl;


    // Initialize Serial Connection
    Connect(&c);
    
    if (c.connection_init == true)
    {
        InitForceMode(&f)
    }
    else 
    {
        //ADD CODE STOP
        cout << "Reading thread could not be initialized, exiting..." << endl;
    }
        
    while (fb.flag == 0)
    {
        ApplyU();
    }
    
    cout << "Exiting Main Control Loop" << endl;
